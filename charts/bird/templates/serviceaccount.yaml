{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bird.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "bird.labels" . | nindent 4 }}
{{- end }}
